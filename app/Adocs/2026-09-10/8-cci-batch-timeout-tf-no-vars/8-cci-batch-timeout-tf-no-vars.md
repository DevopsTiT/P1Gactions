# CCI Batch Timeout TF No Vars

Single file, no `variables.tf` / `tfvars`. Replace `REPLACE_WITH_PAGERDUTY_INTEGRATION_KEY` before apply.

| File | Purpose |
| --- | --- |
| `alerting_cci_aws_batch_time_out.tf` | Provider + alert (all values inline) |
