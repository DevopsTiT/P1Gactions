# Manage ServiceNow + Dynatrace — UI path reminders (review before run)
echo "ServiceNow Store → install Dynatrace Incident Integration → Guided Setup"
echo "ServiceNow → create integration user + transform map + assignment groups"
echo "Dynatrace → Settings → Alerting → Alerting profiles → prod + severity filter"
echo "Dynatrace → Settings → Integration → Problem notifications → ServiceNow → Send ITSM ON → Test → Save"
echo "Optional → Service Graph Connector for Observability Dynatrace → sync CIs"
echo "Weekly → test notify + import set errors + CI link rate + no STG flood"
echo "Docs → https://docs.dynatrace.com/docs/analyze-explore-automate/notifications-and-alerting/problem-notifications/servicenow-integration"
