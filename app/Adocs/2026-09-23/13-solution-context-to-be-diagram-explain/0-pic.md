# Pic — TO-BE Solution Context

```
Dynatrace (+ problem URL)
  → optional EO (noise)
  → PagerDuty Incident hub
       ├── SRE (triage, 4/ask)
       ├── Scribe (transcript, ~6/30m+2)
       ├── Shift (coverage, 0)
       └── Insights (MTTR Q&A, 0)
  → Human decides → Resolve → SRE memory

AS-IS gap closed = assist layer (four agents)
```
