# Create Connection — Pic

```
SNOW user ready?
  → Workflows Authorization: app-settings:objects:read
  → Settings → Connections → ServiceNow → Create
  → name + URL + Basic/OAuth
  → Share → map on workflow SNOW tasks
```
