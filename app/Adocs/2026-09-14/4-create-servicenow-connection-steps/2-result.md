# Result

Create the Connection under Settings → Connections → ServiceNow with instance URL and Basic or OAuth credentials, enable `app-settings:objects:read`, share the Connection, then select it on workflow ServiceNow tasks.
