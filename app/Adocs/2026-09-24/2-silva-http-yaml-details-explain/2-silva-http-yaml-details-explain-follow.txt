Explain the 2 SILVA HTTP + PD YAMLs (detail)

PAIR
1-open-…yaml  = Problem OPEN  → create INC + PD trigger
2-close-…yaml = Problem CLOSE → resolve INC + PD resolve

SHAPE (both)
prepare task first
then two HTTP tasks in parallel (SILVA left, PD right)

SYNC (both must match)
correlation_id = problemId
dedup_key = dt-problem-<problemId>

OPEN TASKS
1 prepare-payload — read Problem, map severity, build sync keys
2 post-silva-incident-http — POST /api/now/v2/table/incident
3 trigger-pagerduty — POST /v2/enqueue event_action=trigger

CLOSE TASKS
1 prepare-close-ids — same sync keys
2 resolve-silva-incident-http — GET by correlation_id → PATCH state 6
   (soft miss if no INC — SILVA may have skipped)
3 resolve-pagerduty — event_action=resolve

BEFORE ACTIVATE
Allowlist silvastg.service-now.com + events.pagerduty.com
Replace __SNOW_PASSWORD__ and __PD_ROUTING_KEY__ in BOTH files
Classic SNOW ITSM notification OFF
Activate BOTH workflows

Full write-up:
Daily Files/2026-09-24/2-silva-http-yaml-details-explain/
