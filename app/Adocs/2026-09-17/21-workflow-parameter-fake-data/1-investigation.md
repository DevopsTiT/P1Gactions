# Investigation

Listed every parameter required by the Connector + PagerDuty workflows (seq 19) and supplied fake example values for each.

Categories: Connection setup, YAML placeholders, Problem event inputs, prepare outputs, snow-create inputs, PD body, cross-link, close path.
