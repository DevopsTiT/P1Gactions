# Pic — Four Tools Common Example

```
Checkout errors
  → Dynatrace Problem
      ├─ ServiceNow INC (track)
      └─ PagerDuty page (wake)
           → Human: Dynatrace scope + Splunk logs
           → Fix
           → Problem close → resolve INC + PD
```
