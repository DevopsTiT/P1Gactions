Why YAML has no snow task after enabling servicenowstg pic

Problem notification ≠ workflow task.
ITSM ON creates SNOW INC automatically — nothing to put in YAML.

Do NOT change YAML to add snow-create-incident while ITSM ON (duplicates).

Keep:
- servicenowstg ITSM ON + ITOM ON (pic)
- seq 15 YAML (PD only, no snow task)

Want snow task on canvas?
- Use seq 13 Connection YAML
- Set notification ITSM OFF

Guide: Daily Files/2026-09-17/16-why-no-snow-task-in-yaml/
