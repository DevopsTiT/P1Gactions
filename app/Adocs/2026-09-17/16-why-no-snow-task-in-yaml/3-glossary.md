# Glossary

| Term | What it means |
| --- | --- |
| Problem notification | Settings auto-push (not a workflow task) |
| Workflow task | Step listed under `tasks:` in YAML |
| snow-create-incident | Connection action only |
| Duplicate INC | ITSM ON + Connection create both fire |
