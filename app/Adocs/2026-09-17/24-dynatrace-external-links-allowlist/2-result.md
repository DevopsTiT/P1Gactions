# Result

Grant these two hostnames in **Settings → External requests**:

| Hostname | Purpose |
| --- | --- |
| `silvastg.service-now.com` | ServiceNow Connector (+ optional classic notification) |
| `events.pagerduty.com` | PagerDuty Events API via JS `fetch` |

Enter host only (no `https://`, no path).
