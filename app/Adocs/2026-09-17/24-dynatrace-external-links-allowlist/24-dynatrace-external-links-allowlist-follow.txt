Dynatrace external links to grant (allowlist)

Settings → External requests → add hostnames (no https://, no path):

REQUIRED for Connector + PagerDuty pack:
  silvastg.service-now.com
    → ServiceNow Connector snow-* APIs
    → also classic Problem notification to same instance

  events.pagerduty.com
    → JS fetch POST /v2/enqueue (trigger + resolve)

NOT required (text-only in tickets):
  Dynatrace Problem URL
  Confluence runbook URL

Common mistakes:
  Do not paste https:// or /v2/enqueue into allowlist
  SNOW-only allowlist → PD fails
  PD-only allowlist → Connector fails

Connection still separate:
  URL https://silvastg.service-now.com + user/password in Connections UI

Full write-up:
  Daily Files/2026-09-17/24-dynatrace-external-links-allowlist/
