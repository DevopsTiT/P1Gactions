Pic 2 (pending) should match Pic 1 (failed) DQL pattern

Pic1 failed ends with:
  stats started/completed/failed by cmxDocumentId
  filter failed > 0

Pic2 pending was wrong (only fields + sort). Change search_query to:

fetch logs
| filter contains(aws.log_group, "/aws/lambda/document-upload-api-prod-createDocumentAction")
| filter contains(content, "action:") and contains(content, "cmxDocumentId:")
| parse content, "LD 'action:' WORD:action LD 'cmxDocumentId:' WORD:cmxDocumentId"
| stats
    count(if(action == "UPLOAD_STARTED", 1, null)) as started,
    count(if(action == "UPLOAD_COMPLETED", 1, null)) as completed,
    count(if(action == "UPLOAD_FAILED", 1, null)) as failed,
    by: {cmxDocumentId}
| filter started > 0 and completed == 0 and failed == 0
| sort started desc
| limit 100

Keep pending alert_name, description, cron */5, LAST_15_MINUTES, expires 30, trigger_conditions.

TF stub:
Daily Files/2026-09-17/1-cdus-pending-match-failed-pattern/cdus_backend_pending_uploads.tf
