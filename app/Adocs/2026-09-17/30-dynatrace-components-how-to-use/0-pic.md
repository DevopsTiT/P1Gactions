# Pic — Dynatrace Components

```
Collect (OneAgent/OTel)
  → Transport (ActiveGate optional)
  → Platform (Smartscape + Grail data)
  → Davis → Problem
       ├─ UI / DQL investigate
       └─ Workflows → SNOW + PD
```
