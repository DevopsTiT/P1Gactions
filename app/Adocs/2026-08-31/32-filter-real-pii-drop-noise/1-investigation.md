# Investigation

Screenshot rows are mostly axacleansingservice operational lines with CleansingResult / counters.
Broad keywords (service name, short tokens like dob) pull those in.
Real PII earlier looked like `address, Value = …`.
