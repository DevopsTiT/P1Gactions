# Glossary

| Term | What it means |
| --- | --- |
| Value = | Log pattern where a field is followed by its actual data |
| CleansingResult | Job status code (0 / 0.0), not customer PII |
| Negative filter | `not contains` — removes known noise lines |
| Noise | Matches that are not useful PII evidence |
