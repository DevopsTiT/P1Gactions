# Result

1. Clear query box completely.
2. Run Step A (host only, 24h, limit 5).
3. Run Step B (`contains` package name, 24h).
4. Run Step C (`contains` `address, Value =`, 24h).
5. Do not reuse any `matchesRegex` block that includes `\s`.
