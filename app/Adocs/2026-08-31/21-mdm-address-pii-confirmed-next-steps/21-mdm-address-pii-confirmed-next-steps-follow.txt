CONFIRMED: MDM address PII in Dynatrace logs

What you found:
  Host group: C_ALI_BU_DATAENGI_A_CUSTMDMGM_E_PRD_T_APP
  Component: jp.co.axa.mdm.ws.composite.axacleansingservice
  Classes: AXACleansingAddressBP / CleansingService
  Leak: INFO logs contain "address, Value = ..." (real address text)

DO NOT:
  Paste full log content into chat/tickets
  Export thousands of raw lines
  Keep browsing more unmasked addresses

DO NEXT — count only (Logs → Advanced):

1) Volume
fetch logs, from: now()-24h
| filter matchesValue(dt.host_group.id, "C_ALI_BU_DATAENGI_A_CUSTMDMGM_E_PRD_T_APP")
| filter matchesPhrase(content, "axacleansingservice")
| filter matchesPhrase(content, "address")
| summarize hit_count = count()

2) Host + file
fetch logs, from: now()-24h
| filter matchesValue(dt.host_group.id, "C_ALI_BU_DATAENGI_A_CUSTMDMGM_E_PRD_T_APP")
| filter matchesPhrase(content, "axacleansingservice")
| filter matchesPhrase(content, "address")
| summarize hit_count = count(), by: { host.name, log.source }
| sort hit_count desc

3) Ticket app owner (no raw address):
  Finding: MDM PRD logs address Value in INFO cleansing BP logs
  Host group: C_ALI_BU_DATAENGI_A_CUSTMDMGM_E_PRD_T_APP
  Ask: stop logging full address Value; or mask before write

FIX (best → backup):
  1 App logger — do not log address Value at INFO
  2 OneAgent sensitive-data masking
  3 OpenPipeline replacePattern at ingest
  4 DQL display mask alone = NOT enough

After fix: re-run count; optional sample limit 5 to confirm *** only

Guides:
  2026-08-31/21-mdm-address-pii-confirmed-next-steps/
  2026-08-28/1-dynatrace-logs-pii-filter/
