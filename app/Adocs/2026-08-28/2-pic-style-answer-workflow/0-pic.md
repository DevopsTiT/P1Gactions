# 0 Pic Overview

## Decision tree (pic)

```
new Cursor question today?
  → list today's folders under Files/<date>/ and Adocs/<date>/
  → next free integer = seq
  → slug = 3–6 word kebab from question
  → create both folders
  → write 0/1/2/3 + main md + sh + glossary + follow
  → paste full answer in chat
```

## Pic data flow

```
Ask
 → Seq folder
 → 0 pic
 → 1 investigation
 → 2 result
 → 3 glossary
 → sh commands
 → dual write (Files + Adocs)
 → chat follow (desktop/mobile/cloud)
```

## Why pic first

| Reason | What it means |
|--------|---------------|
| Fast scan | Arrows and trees beat long prose on mobile |
| Shared language | Same pic block in main md and `0-pic.md` |
| On-call friendly | Symptom → check → fix fits one screen |
