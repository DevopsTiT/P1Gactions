# Result

Best practise: one job per workflow, filtered triggers, secrets via Connection/UI (not Git), stable Problem ID keys, non-prod test of open and close, then tighten for prod. For SNOW+PD keep two workflows and prefer Connection for ServiceNow.
