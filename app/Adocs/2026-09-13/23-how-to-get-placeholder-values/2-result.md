# Result

Get instance URL from the browser host; user/password from a SNOW integration account; PD key from Events API v2 on the target service; every `*_SYS_ID_*` by opening the matching SNOW record and copying `sys_id` from the URL (or asking admin).
