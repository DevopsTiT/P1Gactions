# Result

This YAML is the readable **create** workflow template for Problem open: template wrapper + four JS tasks (prepare, parallel SNOW+PD, cross-link). Same logic as the create JSON; replace placeholders, upload as template, and pair with the close YAML for full lifecycle.
