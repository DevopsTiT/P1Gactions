# AGO Create YAML — Pic

```
ago-problem-to-snow-pagerduty.workflow-template.yaml
  = CREATE template (OPEN only)
  wrapper: version + dynatrace.automations dep
  same 4 tasks as JSON twin
  prepare → [SNOW || PD] → cross-link
```
