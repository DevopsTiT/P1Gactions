# Pic

```
Settings → Collect and capture → Log monitoring
  → Configure log module → Sensitive data masking
  → New rule
  → regex "key":"(.*?)" → ***
  → matcher / host group C_ALJ_BU_…
  → Save → fake test log → confirm ***
```
