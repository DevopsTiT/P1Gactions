OneAgent PII masking — step by step

PATH
Settings → Collect and capture → Log monitoring → Configure log module → Sensitive data masking

SCOPE
Start on one Host group (e.g. C_ALJ_BU_BAP_A_TAXPAYMENT_E_PRD), then expand.

EACH RULE
1. New rule
2. Name: pii-mask-<key>
3. Masking type: STRING
4. Replacement: ***
5. Search expression: "<key>"\s*:\s*"(.*?)"
6. Matcher: process group / log.source / host.tag as needed
7. Save

FIRST RULES
- Email generic: \b[\w\-\._]+?@[\w\-\._]+?\.\w{2,10}\b
- policyHolderName, subscriberDOB, bankAccountNo, telephoneNumber, EmailAddress1
- Then waves A–D in 9-oneagent-pii-regex-waves.txt

TEST
Write fake JSON log with TEST_NAME / fake@example.com
Wait 2–5 min → Logs search → must see *** not cleartext

NOTE
Only masks OneAgent-collected logs. API/Fluent → OpenPipeline.
Max one capture group per regex.

Doc: Daily Files/2026-09-06/9-oneagent-pii-masking-steps/
