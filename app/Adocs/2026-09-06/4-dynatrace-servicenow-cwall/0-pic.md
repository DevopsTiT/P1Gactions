# Pic

```
CWall = ?
  CMDB? → Dynatrace syncs hosts/services into ServiceNow inventory
  Wallboard? → Screen showing DT problems + SNOW tickets
  Unknown internal code? → ask team name
```

```
Dynatrace (detect + RCA)
  → notification
  → ServiceNow (incident)
  → CMDB CI (who/what)
  → fix → close both
```
