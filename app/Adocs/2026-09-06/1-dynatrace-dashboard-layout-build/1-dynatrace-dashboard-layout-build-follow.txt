Dynatrace dashboard — match DASHBOARD LAYOUT VISUAL (11 rows)

Goal: Build one board that follows the picture top to bottom.

ROW MAP
1 APPLICATION SELECTOR | [All][EIP][API][Payment]...
   → Dashboard variable `app`. Every data tile filters by it.
2 CRITICAL ALERTS & AI ROOT CAUSE
   → Markdown header only
3 Problems | Root Cause | Alert Summary
   → Problems list + Davis/root-cause view + open-problems-by-severity
4 APPLICATION PERFORMANCE METRICS
   → Markdown header
5 Requests | Errors | Response Time
   → 3 timeseries (throughput, failure rate, response time)
6 INFRASTRUCTURE STATUS
   → Markdown header
7 Windows CPU | Linux CPU | Memory
   → Host CPU split by OS + memory %
8 LOGS & ERRORS
   → Markdown header
9 Error Logs | Exception Traces
   → Logs ERROR table + Exception/stack query
10 DATABASE & DEPENDENCIES
   → Markdown header
11 DB Connections | Queries | External Services
   → DB pool/connections, query time, called services errors/latency

BUILD ORDER
1) Create dashboard App-Health-Layout (or App-Health-STG)
2) Add variable app = All, EIP, API, Payment
3) Tag services/hosts app:eip / app:api / app:payment
4) Add Markdown headers (rows 2,4,6,8,10)
5) Add data tiles (rows 3,5,7,9,11) with $app filter
6) Default timeframe Last 2 hours
7) Pass checklist: selector works; no cross-app bleed; all labels present

PREREQ
- OneAgent (or OTel) metrics present
- Logs ingested for Error/Exception rows
- DB/external calls visible in service flow

PASS RULE
Changing app chip must change Problems + KPIs + hosts + logs + DB tiles together.

Saved: Daily Files/2026-09-06/1-dynatrace-dashboard-layout-build/
