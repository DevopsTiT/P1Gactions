# Investigation

User provided DASHBOARD LAYOUT VISUAL PNG with exactly 11 rows. Mapped each label to Dynatrace tile types (Markdown headers, Problems, Data explorer timeseries, Logs, service-flow backends). Distinguished from older L1-Health 11-section list (hosts/AWS/architecture) — this build follows the new visual only.
