# Pic

```
Row1  App selector (All/EIP/API/Payment)
  → Row2–3  Problems | Root Cause | Alert Summary
  → Row4–5  Requests | Errors | Response Time
  → Row6–7  Win CPU | Linux CPU | Memory
  → Row8–9  Error Logs | Exception Traces
  → Row10–11 DB Connections | Queries | External Services
```

```
Need empty tile?
  wrong $app tag → fix tag
  no OneAgent → fix ingest first
  no logs → enable log ingest
  no DB → pin called service / stub
```
