# Pic

```
Need from Dynatrace.txt
  One main multi-app board? → $app variable (All/EIP/API/Payment)
  Select EIP → servers+logs+errors? → filter same tiles
  Win + Linux? → hosts table + Win/Linux CPU + Memory
  EIP dedicated board? → NO — filter only
  AI RCA right away? → Davis row at top
```
