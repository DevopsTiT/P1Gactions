# Pic — How Main Multi-App Dashboard

```
Need: one board + select EIP → servers/logs/errors
  │
  ├─ Multi dashboards per app? → NO
  └─ One board + $app variable? → YES
        Tag → Import JSON → Select EIP → Verify
```

```
$app=All  → everything
$app=EIP  → multi EIP hosts + EIP logs + EIP errors (same board)
```
