# Investigation

| Source | Finding |
| --- | --- |
| User image / Dynatrace.txt | One main multi-app dashboard; select EIP → multi servers, logs, errors |
| Existing work | `Main-MultiApp-Health-Dashboards.json` already implements `$app` filter |
| Gap for user | Needs clear **how-to** steps (tag → import → select → verify) |
| Anti-pattern | Cloning one dashboard per application |
