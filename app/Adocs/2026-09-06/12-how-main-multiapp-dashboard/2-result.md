# Result

1. Tag hosts/services `app:eip` (and api/payment).
2. Import `Main-MultiApp-Health-Dashboards.json` as **one** board.
3. Use dropdown `app = EIP` — do not create a second EIP dashboard.
4. Confirm multi hosts + Error Logs + Errors KPI update together.
5. If empty: fix names/tags, OneAgent, or log ingest — not more boards.
