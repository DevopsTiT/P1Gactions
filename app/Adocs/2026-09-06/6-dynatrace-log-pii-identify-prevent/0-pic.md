# Pic

```
Push logs → Dynatrace
  Scan DQL with pii-keys?
    hit → contain (OpenPipeline) + fix app + cleanup process
    miss → still deploy prevent rules
```

```
Prevent forever
  App deny-list
    → OneAgent mask
    → OpenPipeline remove/mask
    → weekly scan
```
