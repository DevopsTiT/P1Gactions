# LocalStack = local fake AWS API
services:
  localstack:
    image: localstack/localstack:4.0
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,ecr,iam,sts,ec2
      - DEBUG=0
    volumes:
      - "./localstack-data:/var/lib/localstack"
